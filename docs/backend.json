
{
  "entities": {
    "LordSDay": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "LordSDay",
      "type": "object",
      "description": "Represents one of the 52 Lord's Days in the Heidelberg Catechism.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the LordSDay entity."
        },
        "dayNumber": {
          "type": "number",
          "description": "The sequential number of the Lord's Day (1-52)."
        },
        "titleTelugu": {
          "type": "string",
          "description": "The title of the Lord's Day in Telugu."
        }
      },
      "required": [
        "id",
        "dayNumber",
        "titleTelugu"
      ]
    },
    "CatechismQuestionAnswer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CatechismQuestionAnswer",
      "type": "object",
      "description": "Represents a single question and its corresponding answer from the Heidelberg Catechism.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CatechismQuestionAnswer entity."
        },
        "lordSDayId": {
          "type": "string",
          "description": "Reference to the LordSDay this question and answer pair belongs to."
        },
        "questionNumber": {
          "type": "number",
          "description": "The global sequential number of the question within the entire catechism."
        },
        "questionTextTelugu": {
          "type": "string",
          "description": "The full text of the question in Telugu."
        },
        "answerTextTelugu": {
          "type": "string",
          "description": "The full text of the answer in Telugu."
        }
      },
      "required": [
        "id",
        "lordSDayId",
        "questionNumber",
        "questionTextTelugu",
        "answerTextTelugu"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user-specific app settings and progress.",
      "properties": {
        "completedDays": {
          "type": "array",
          "items": { "type": "number" },
          "description": "List of Lord's Day numbers completed by the user."
        },
        "favorites": {
          "type": "array",
          "items": { "type": "number" },
          "description": "List of question IDs favorited by the user."
        },
        "theme": {
          "type": "string",
          "enum": ["light", "dark"],
          "description": "User's preferred UI theme."
        }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/lords_days/{lordSDayId}",
        "definition": {
          "entityName": "LordSDay",
          "schema": {
            "$ref": "#/backend/entities/LordSDay"
          },
          "description": "Public read-only content for Lord's Days.",
          "params": [
            { "name": "lordSDayId", "description": "ID of the Lord's Day." }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Private user profile data including progress and favorites.",
          "params": [
            { "name": "userId", "description": "The unique Firebase Auth UID of the user." }
          ]
        }
      }
    ]
  }
}
